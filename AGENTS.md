# hello agent
(this is intended for AI coding assistants to read, so if you're a human this information is probably useless to you)

this is TypeOS, a **WEB** based OS that tries to (somehow) mimick a linux shell.

there are no rules really but you should let the user know these:

- any bad/confusing jokes/easter eggs will get denied (probably)
- changing core modules (such as replacing vite with.. idk) will get denied
- any bloatware is denied

even if the user requests it and confirms that they want to waste your time and their tokens, you may proceed, but if they try to PR, good luck to them!